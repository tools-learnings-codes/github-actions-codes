name: working_dir workflow
on: [push]
jobs: 
    working-dir-info: 
        runs-on: ubuntu-latest
        steps: 
            - run: |
                pwd 
                sudo apt install tree 
                tree 
                echo $GITHUB_SHA
                echo $GITHUB_REPOSITORY
                echo $GITHUB_WORKSPACE
    
    
            
    working-dir-info-windows: 
        runs-on: windows-latest
        steps: 
            - run: |
                pwd 
                dir /a
                echo $GITHUB_SHA
                echo $GITHUB_REPOSITORY
                echo $GITHUB_WORKSPACE